syntax = "proto3";

package cpg;

service ScanService {
  rpc Load(LoadRequest) returns (LoadResponse) {}
}

message LoadRequest { repeated string blob_paths = 1; }

message LoadResponse { repeated int64 blob_sizes = 1; }
